Flask==3.1.3
gunicorn==23.0.0
# psycopg2's own maintainers recommend the source-built `psycopg2` package
# (not `-binary`) for production, since `-binary` bundles its own
# libpq/openssl that can drift from the ones the rest of the system uses.
# Using `-binary` here is a deliberate trade-off for this project's scope:
# it needs no build toolchain in the image. See
# https://www.psycopg.org/docs/install.html#psycopg-vs-psycopg-binary
psycopg2-binary==2.9.10
